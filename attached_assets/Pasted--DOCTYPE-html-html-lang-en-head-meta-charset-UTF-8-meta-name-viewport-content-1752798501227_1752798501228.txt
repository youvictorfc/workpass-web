<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WorkPass Australia - Your Construction Work-Readiness Passport</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-blue: #0052CC;
            --secondary-blue: #2684FF;
            --accent-green: #00875A;
            --dark-navy: #172B4D;
            --light-gray: #F4F5F7;
            --border-gray: #DFE1E6;
            --text-primary: #172B4D;
            --text-secondary: #6B778C;
            --warning-orange: #FF8B00;
            --success-green: #00875A;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            line-height: 1.6;
            color: var(--text-primary);
            background-color: #FFFFFF;
        }

        /* Header */
        .header {
            background: #FFFFFF;
            box-shadow: 0 1px 2px rgba(0,0,0,0.05);
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 72px;
        }

        .logo {
            display: flex;
            align-items: center;
            font-weight: 700;
            font-size: 20px;
            color: var(--primary-blue);
        }

        .logo-icon {
            width: 32px;
            height: 32px;
            background: var(--primary-blue);
            border-radius: 8px;
            margin-right: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 900;
        }

        .nav-links {
            display: flex;
            gap: 32px;
            align-items: center;
        }

        .nav-links a {
            color: var(--text-primary);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.2s;
        }

        .nav-links a:hover {
            color: var(--primary-blue);
        }

        .cta-button {
            background: var(--primary-blue);
            color: white;
            padding: 10px 24px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.2s;
        }

        .cta-button:hover {
            background: #0747A6;
            transform: translateY(-1px);
        }

        /* Hero Section */
        .hero {
            padding: 140px 24px 80px;
            background: linear-gradient(135deg, #F4F5F7 0%, #FFFFFF 100%);
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -10%;
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(38, 132, 255, 0.05) 0%, transparent 70%);
            border-radius: 50%;
        }

        .hero-content {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 80px;
            align-items: center;
            position: relative;
            z-index: 1;
        }

        .hero-text h1 {
            font-size: 48px;
            font-weight: 800;
            line-height: 1.2;
            margin-bottom: 24px;
            color: var(--dark-navy);
        }

        .hero-text .highlight {
            color: var(--primary-blue);
        }

        .hero-text p {
            font-size: 20px;
            color: var(--text-secondary);
            margin-bottom: 32px;
            line-height: 1.6;
        }

        .hero-actions {
            display: flex;
            gap: 16px;
            align-items: center;
        }

        .secondary-button {
            padding: 10px 24px;
            border: 2px solid var(--border-gray);
            border-radius: 6px;
            text-decoration: none;
            color: var(--text-primary);
            font-weight: 600;
            transition: all 0.2s;
        }

        .secondary-button:hover {
            border-color: var(--primary-blue);
            color: var(--primary-blue);
        }

        .hero-visual {
            position: relative;
        }

        .credential-card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.1);
            padding: 32px;
            position: relative;
            transform: rotate(-2deg);
            transition: transform 0.3s;
        }

        .credential-card:hover {
            transform: rotate(-1deg) scale(1.02);
        }

        .badge-preview {
            text-align: center;
            padding: 24px;
            background: var(--light-gray);
            border-radius: 12px;
            margin-bottom: 24px;
        }

        .badge-icon {
            width: 80px;
            height: 80px;
            background: var(--success-green);
            border-radius: 50%;
            margin: 0 auto 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
            color: white;
        }

        .readiness-meter {
            background: var(--light-gray);
            height: 12px;
            border-radius: 6px;
            overflow: hidden;
            margin-top: 16px;
        }

        .readiness-fill {
            height: 100%;
            width: 75%;
            background: var(--success-green);
            transition: width 0.5s;
        }

        /* Stats Section */
        .stats {
            padding: 80px 24px;
            background: var(--dark-navy);
            color: white;
        }

        .stats-container {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 48px;
            text-align: center;
        }

        .stat-item h3 {
            font-size: 48px;
            font-weight: 800;
            margin-bottom: 8px;
            color: var(--secondary-blue);
        }

        .stat-item p {
            font-size: 16px;
            opacity: 0.8;
        }

        /* Features Section */
        .features {
            padding: 80px 24px;
            background: #FFFFFF;
        }

        .features-container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .section-header {
            text-align: center;
            margin-bottom: 64px;
        }

        .section-header h2 {
            font-size: 40px;
            font-weight: 800;
            margin-bottom: 16px;
            color: var(--dark-navy);
        }

        .section-header p {
            font-size: 18px;
            color: var(--text-secondary);
            max-width: 600px;
            margin: 0 auto;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 32px;
        }

        .feature-card {
            background: var(--light-gray);
            border-radius: 12px;
            padding: 32px;
            transition: all 0.3s;
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: var(--primary-blue);
            transform: translateX(-100%);
            transition: transform 0.3s;
        }

        .feature-card:hover::before {
            transform: translateX(0);
        }

        .feature-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px rgba(0,0,0,0.1);
        }

        .feature-icon {
            width: 48px;
            height: 48px;
            background: var(--primary-blue);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 24px;
            margin-bottom: 16px;
        }

        .feature-card h3 {
            font-size: 20px;
            font-weight: 700;
            margin-bottom: 12px;
            color: var(--dark-navy);
        }

        .feature-card p {
            color: var(--text-secondary);
            line-height: 1.6;
        }

        /* Process Section */
        .process {
            padding: 80px 24px;
            background: var(--light-gray);
        }

        .process-steps {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 32px;
            max-width: 1200px;
            margin: 0 auto;
            position: relative;
        }

        .process-steps::before {
            content: '';
            position: absolute;
            top: 40px;
            left: 10%;
            right: 10%;
            height: 2px;
            background: var(--border-gray);
            z-index: 0;
        }

        .step {
            text-align: center;
            position: relative;
            z-index: 1;
        }

        .step-number {
            width: 80px;
            height: 80px;
            background: white;
            border: 3px solid var(--primary-blue);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            font-weight: 800;
            color: var(--primary-blue);
            margin: 0 auto 24px;
        }

        .step h4 {
            font-size: 18px;
            font-weight: 700;
            margin-bottom: 8px;
            color: var(--dark-navy);
        }

        .step p {
            font-size: 14px;
            color: var(--text-secondary);
        }

        /* CTA Section */
        .cta-section {
            padding: 80px 24px;
            background: var(--primary-blue);
            color: white;
            text-align: center;
        }

        .cta-content {
            max-width: 800px;
            margin: 0 auto;
        }

        .cta-content h2 {
            font-size: 40px;
            font-weight: 800;
            margin-bottom: 24px;
        }

        .cta-content p {
            font-size: 20px;
            margin-bottom: 32px;
            opacity: 0.9;
        }

        .cta-white {
            background: white;
            color: var(--primary-blue);
            padding: 14px 32px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 700;
            font-size: 18px;
            display: inline-block;
            transition: all 0.2s;
        }

        .cta-white:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 16px rgba(0,0,0,0.2);
        }

        /* Footer */
        .footer {
            padding: 48px 24px;
            background: var(--dark-navy);
            color: white;
        }

        .footer-container {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 48px;
        }

        .footer-brand p {
            opacity: 0.7;
            margin-top: 16px;
            line-height: 1.6;
        }

        .footer-section h4 {
            font-size: 16px;
            font-weight: 700;
            margin-bottom: 16px;
        }

        .footer-section ul {
            list-style: none;
        }

        .footer-section li {
            margin-bottom: 12px;
        }

        .footer-section a {
            color: white;
            text-decoration: none;
            opacity: 0.7;
            transition: opacity 0.2s;
        }

        .footer-section a:hover {
            opacity: 1;
        }

        .trust-badges {
            display: flex;
            gap: 16px;
            margin-top: 24px;
        }

        .trust-badge {
            background: rgba(255,255,255,0.1);
            padding: 8px 16px;
            border-radius: 4px;
            font-size: 12px;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .hero-content {
                grid-template-columns: 1fr;
                gap: 40px;
            }

            .hero-text h1 {
                font-size: 36px;
            }

            .stats-container,
            .features-grid,
            .process-steps {
                grid-template-columns: 1fr;
            }

            .footer-container {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="header">
        <nav class="nav-container">
            <div class="logo">
                <div class="logo-icon">WP</div>
                WorkPass Australia
            </div>
            <div class="nav-links">
                <a href="#features">Features</a>
                <a href="#how-it-works">How It Works</a>
                <a href="#for-business">For Business</a>
                <a href="#" class="cta-button">Get Started</a>
            </div>
        </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <div class="hero-text">
                <h1>Your Digital <span class="highlight">Work-Readiness Passport</span> for Australian Construction</h1>
                <p>Get site-ready in under 24 hours. Upload credentials, complete training, and showcase your qualifications to employers—all in one secure platform.</p>
                <div class="hero-actions">
                    <a href="#" class="cta-button">Start Your Journey</a>
                    <a href="#" class="secondary-button">Learn More</a>
                </div>
            </div>
            <div class="hero-visual">
                <div class="credential-card">
                    <div class="badge-preview">
                        <div class="badge-icon">✓</div>
                        <h3>Work-Ready Status</h3>
                        <p>John Smith</p>
                        <p style="font-size: 14px; color: var(--text-secondary);">White Card Verified</p>
                    </div>
                    <div>
                        <p style="font-weight: 600; margin-bottom: 8px;">Readiness Level</p>
                        <div class="readiness-meter">
                            <div class="readiness-fill"></div>
                        </div>
                        <p style="font-size: 14px; color: var(--text-secondary); margin-top: 8px;">75% Complete</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Stats Section -->
    <section class="stats">
        <div class="stats-container">
            <div class="stat-item">
                <h3><24h</h3>
                <p>Time to Work-Ready</p>
            </div>
            <div class="stat-item">
                <h3>50%</h3>
                <p>Less Admin Time</p>
            </div>
            <div class="stat-item">
                <h3>100%</h3>
                <p>Compliance Assured</p>
            </div>
            <div class="stat-item">
                <h3>90%</h3>
                <p>Faster Job Matching</p>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features" id="features">
        <div class="features-container">
            <div class="section-header">
                <h2>Everything You Need to Start Working</h2>
                <p>A complete platform designed to get you site-ready faster and keep you compliant throughout your career</p>
            </div>
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">📋</div>
                    <h3>Guided Checklist</h3>
                    <p>Role-specific pathway showing exactly what credentials and training you need to get started</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🔍</div>
                    <h3>Instant Verification</h3>
                    <p>Upload documents and get verified status in 30 seconds with our automated validation system</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">📱</div>
                    <h3>Digital Badge</h3>
                    <p>Share your work-ready status on social media and with employers in one click</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">⏰</div>
                    <h3>Smart Reminders</h3>
                    <p>Never miss an expiry date with automated notifications 30, 7, and 1 day before renewal</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🎯</div>
                    <h3>Job Matching</h3>
                    <p>Get matched with verified employers looking for workers with your exact qualifications</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🔒</div>
                    <h3>Secure Storage</h3>
                    <p>Bank-level security keeps your documents safe and accessible whenever you need them</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Process Section -->
    <section class="process" id="how-it-works">
        <div class="section-header">
            <h2>Get Site-Ready in 4 Simple Steps</h2>
            <p>Our streamlined process gets you from signup to job-ready faster than ever</p>
        </div>
        <div class="process-steps">
            <div class="step">
                <div class="step-number">1</div>
                <h4>Sign Up</h4>
                <p>Create your account with email or phone verification</p>
            </div>
            <div class="step">
                <div class="step-number">2</div>
                <h4>Upload Documents</h4>
                <p>Scan and upload your White Card and other credentials</p>
            </div>
            <div class="step">
                <div class="step-number">3</div>
                <h4>Get Verified</h4>
                <p>Instant validation through official databases</p>
            </div>
            <div class="step">
                <div class="step-number">4</div>
                <h4>Start Working</h4>
                <p>Share your badge and connect with employers</p>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
        <div class="cta-content">
            <h2>Ready to Fast-Track Your Construction Career?</h2>
            <p>Join thousands of workers getting site-ready in under 24 hours</p>
            <a href="#" class="cta-white">Get Started Free</a>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-container">
            <div class="footer-brand">
                <div class="logo" style="color: white;">
                    <div class="logo-icon">WP</div>
                    WorkPass Australia
                </div>
                <p>The trusted digital passport for Australia's construction workforce. Keeping workers skilled, safe, and site-ready.</p>
                <div class="trust-badges">
                    <div class="trust-badge">ISO 27001</div>
                    <div class="trust-badge">SafeWork NSW Partner</div>
                </div>
            </div>
            <div class="footer-section">
                <h4>Platform</h4>
                <ul>
                    <li><a href="#">Features</a></li>
                    <li><a href="#">For Workers</a></li>
                    <li><a href="#">For Employers</a></li>
                    <li><a href="#">Pricing</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h4>Resources</h4>
                <ul>
                    <li><a href="#">White Card Guide</a></li>
                    <li><a href="#">Safety Training</a></li>
                    <li><a href="#">Help Center</a></li>
                    <li><a href="#">API Docs</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h4>Company</h4>
                <ul>
                    <li><a href="#">About Us</a></li>
                    <li><a href="#">Privacy Policy</a></li>
                    <li><a href="#">Terms of Service</a></li>
                    <li><a href="#">Contact</a></li>
                </ul>
            </div>
        </div>
    </footer>

    <script>
        // Smooth scroll for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Animate stats on scroll
        const observerOptions = {
            threshold: 0.5,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '0';
                    entry.target.style.transform = 'translateY(20px)';
                    setTimeout(() => {
                        entry.target.style.transition = 'all 0.6s ease-out';
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }, 100);
                    observer.unobserve(entry.target);
                }
            });
        }, observerOptions);

        // Observe all feature cards and steps
        document.querySelectorAll('.feature-card, .step, .stat-item').forEach(el => {
            observer.observe(el);
        });

        // Animate readiness meter on load
        window.addEventListener('load', () => {
            const meter = document.querySelector('.readiness-fill');
            setTimeout(() => {
                meter.style.width = '75%';
            }, 500);
        });
    </script>
</body>
</html>